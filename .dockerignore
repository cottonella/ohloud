# Keep the build context small and reproducible: reinstall deps in-image and
# regenerate the site, so never ship host artifacts.
node_modules
.output
.nuxt
src-tauri
.git
.github
.vscode
.claude
*.log
